document.addEventListener('DOMContentLoaded', () => {
    const card = document.querySelector('.card');
    const releaseForm = document.querySelector('.release-form');
    const assignForm = document.querySelector('.assign-form');

    card.addEventListener('click', () => {
        // Добавьте дополнительные действия при клике на карту
    });

    releaseForm.addEventListener('submit', (e) => {
        e.preventDefault();
        // Добавьте анимацию или обновления при освобождении книги
    });

    assignForm.addEventListener('submit', (e) => {
        e.preventDefault();
        // Добавьте анимацию или обновления при назначении книги
    });

    // Добавьте дополнительные интерактивные функции здесь
});
